export deftype globals <: int<6> (bool Vdd, GND, Reset, _Reset, pReset, sReset, _pReset, _sReset)
{
	sReset = Reset;
	_sReset = _Reset;

	set { }
	get { }
}

export deftype globals_np <: int<4> (bool Reset, _Reset, pReset, sReset, _pReset, _sReset)
{
	sReset = Reset;
	_sReset = _Reset;

	set { }
	get { }
}

pint pn_ratio = 3;
pint min_width = 4;
